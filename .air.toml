root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/chat ./cmd/chat"
  bin = "./tmp/chat"
  include_ext = ["go", "yaml"]
  exclude_dir = ["gen", ".git", "tmp", ".devspace", "charts"]
  delay = 1000

[log]
  time = false
